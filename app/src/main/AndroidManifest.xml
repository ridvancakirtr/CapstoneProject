<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ridvan.doctorandpatientfirebase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminActivity" />
        <activity android:name=".Doctor.DoctorActivity" />
        <activity android:name=".DoctorAddActivity" />
        <activity android:name=".Patient.PatientActivity" />
        <activity android:name=".PatientAddActivity" />
        <activity android:name=".Patient.PatientProfileActivity" />
        <activity android:name=".HospitalAddActivity" />
        <activity android:name=".DistrictAddActivity" />
        <activity android:name=".AllHospitalActivity" />
        <activity android:name=".Patient.PatientLineChartActivity" />
        <activity android:name=".AllDoctorActivity" />
        <activity android:name=".AllPatientActivity" />
        <activity android:name=".Doctor.DoctorProfileActivity"></activity>
    </application>

</manifest>